ImageEncrptor

After execution create a passkey and select encrpt and select a pic and choose a path to save a decrypted path
Same process for image decryption

install cv2 from cmd by using following commend
pip install cvopen-python

then run the code in python IDE